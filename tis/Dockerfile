# Используем официальный образ JDK 17 как базовый
# Таким образом, контейнер уже включает необходимую среду исполнения Java, что позволяет запускать
# Java-приложения без установки дополнительного ПО
FROM openjdk:17-jdk-slim AS base

# Устанавливаем рабочую директорию внутри контейнера
# Директория /app становится рабочей, и дальнейшие операции копирования файлов и команд выполняются относительно неё
WORKDIR /app

# Копируем артефакт приложения (например, JAR-файл) в контейнер
COPY target/tis-0.0.1-SNAPSHOT.jar /app.jar

# Запускаем приложение при запуске контейнера
CMD ["java", "-jar", "/app.jar"]